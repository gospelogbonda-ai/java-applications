// 4.11 – Find the Smallest Value
// Answer/Explanation:
// - Read the first number to know how many values to input.
// - Use a variable to store the smallest value.
// - Compare each input value with the current smallest, updating if a smaller one is found.
// - Display the smallest value at the end.

import java.util.Scanner;

public class SmallestValue {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);

        System.out.print("Enter the number of values: ");
        int n = input.nextInt(); // number of values to read

        System.out.print("Enter value 1: ");
        int smallest = input.nextInt(); // initialize smallest with first value

        for (int i = 2; i <= n; i++) {
            System.out.print("Enter value " + i + ": ");
            int value = input.nextInt();
            if (value < smallest) {
                smallest = value; // update smallest if current value is smaller
            }
        }

        System.out.println("The smallest value is: " + smallest);
        input.close();
    }
}
